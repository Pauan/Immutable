/**
   @function isImmutable
   @param {Any} [x]
   @return {Boolean} `true` if `x` is immutable
   @summary Returns whether `x` is immutable
   @desc
     Strings, numbers, booleans, null, undefined, symbols, frozen
     objects, [::Dict], [::List], [::Queue], [::Record], [::Set],
     [::Stack], [::Tuple], and [::Tag] are immutable.

     Everything else is mutable.
 */
