/**
   @function any
   @param {::Iterable} [x]
   @param {Function} [fn]
   @return {Boolean}
   @summary Returns `true` if `fn` returns `true` for any
            of the values in `x`
   @desc
     This function calls `fn` for each value in `x`, and
     if `fn` returns `true`, it will return `true`.

     If `fn` never returns `true`, then this function returns
     `false`.
 */
