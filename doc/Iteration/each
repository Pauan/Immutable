/**
   @function each
   @param {::Iterable} [x]
   @param {Function} [fn]
   @summary Calls `fn` for each value in `x`
   @desc
     This is the same as a `for..of` loop in ECMAScript 6:

         // 1
         // 2
         // 3
         each([1, 2, 3], function (x) {
           console.log(x);
         });

         // 1
         // 2
         // 3
         each(Tuple([1, 2, 3]), function (x) {
           console.log(x);
         });
 */
