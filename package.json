{
  "private": true,
  "name": "Immutable",
  "version": "1.0.0",
  "repository": "https://github.com/Pauan/Immutable.git",
  "main": "index.js",
  "devDependencies": {
    "es6-module-transpiler": "^0.9.6",
    "uglify-js": "^2.4.16"
  },
  "scripts": {
    "prepublish": "compile-modules convert --output index.js --format bundle src/main.js && uglifyjs --in-source-map index.js.map --source-map index.js.map --screw-ie8 --mangle --compress unsafe --output index.js"
  }
}
